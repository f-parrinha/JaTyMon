import examples.MultiTaskRobotProtocol.MechanicalArm;

typestate MultiTaskRobotProtocol {
  OFF = {
    void turnOn(): IDLE
  }

  IDLE = {
    void turnOff(): OFF,
    void executeTask(String): IDLE,
    void unplugArm(): UNPLUGGED
  }

  UNPLUGGED = {
    void plugArm(MechanicalArm): IDLE
  }
}
